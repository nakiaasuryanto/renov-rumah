{
  "name": "fin-automation",
  "version": "1.0.0",
  "description": "Financial journal automation system",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "mkdir -p public && cp index.html form.js transaksi.html public/",
    "vercel-build": "mkdir -p public && cp index.html form.js transaksi.html public/"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "sqlite3": "^5.1.6",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}